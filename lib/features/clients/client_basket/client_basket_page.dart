import 'package:flutter/material.dart';

class ClientBasketPage extends StatelessWidget {
  const ClientBasketPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Hello Basket Page"),),
    );
  }
}