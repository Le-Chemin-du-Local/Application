import 'package:flutter/material.dart';

class CommercesPage extends StatelessWidget {
  const CommercesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Hello Stores Page")),
    );
  }
}