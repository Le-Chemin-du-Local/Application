import 'package:flutter/material.dart';

class StoreKeeperProductsPage extends StatelessWidget {
  const StoreKeeperProductsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Hello StoreKeeper Products Page")),
    );
  }
}